package com.example.po.personalization

import com.example.po.domain.model.ChatMessage

class BehaviorTracker {
    fun trackUserInteraction(message: ChatMessage) {
        // Track interaction
    }
}
